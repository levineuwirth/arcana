/target
**/*.rs.bk
Cargo.lock

# Profiling artifacts (cargo-flamegraph writes to cwd by default)
perf.data*
flamegraph.svg

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
CLAUDE.md
*.pdf
*.tex
# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.venv/
.pytest_cache/

# maturin / PyO3 build artifacts dropped into the package tree
arcana-py/python/arcana/*.so
arcana-py/python/arcana/*.pyd

# Volatile verify-pipeline scratch file (overwritten every run).
# arcana-cards/build.rs writes a stub on fresh clones so the crate
# still compiles before verify has ever run.
arcana-cards/src/generated/_scratch/candidate.rs
