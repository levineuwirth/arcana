# FIDELITY PAYOFF CHECK: did the fidelity grind move VMC's correlation with the
# MTGTop8 recorded-conversion proxy off the floor?
# Method: run the VMC gauntlet on the RECOVERED relaxed field (32 decks / 15 title
# strings, now synergy-payoff-faithful; shocklands still approximated), correlate
# via the repaired placement_vs_gauntlet.py bucket script, compare to prior VMC
# checks. Build the field: build_fidelity_subset -> FIDELITY_OUT/relaxed/PI;
# gauntlet: corpus_gauntlet CORPUS_REFEREE=vmc CORPUS_DUELS=1 (62 games/deck).
#
# SHORT ANSWER: VMC still fails the external conversion proxy. No global improvement.
#
#   VMC field                              rho vs recorded   rho vs event_win   N buckets
#   62-deck broad (gauntlet-vs-real)          -0.20              -0.30              5
#   12-deck subset (pimc-vs-real)             +0.10              -0.10              5
#   32-deck recovered (this)                  -0.40              -0.20              4
#
# Recovered-field buckets (gauntlet_pr vs recorded / event_win):
#   Gruul/RG Aggro     0.782   target 0.511 / 0.093   <- still over-rated by VMC
#   UW Spirit Aggro    0.651   target 0.594 / 0.192   <- near recorded target
#   Sultai Control     0.476   target 0.512 / 0.186   <- near recorded target
#   Golgari/HardScales 0.368   target 0.526 / 0.104   <- still under-rated by VMC
#
# READ (nuanced):
# - The global rho did not improve; VMC remains a poor external validator.
# - Post-fix Spirits sit near the recorded target, and the implemented anthem cards
#   make the material-visible mechanism plausible. But this is NOT a controlled
#   before/after: the 62-deck pre-fix Spirit bucket was already near the same target,
#   and the fields differ. Treat the causal "anthem fix aligned Spirits" claim as
#   plausible engineering interpretation, not evidence.
# - Fidelity did not rescue grindy synergy under VMC: Golgari/Scales executes better
#   now, but its payoff is incremental/over-time, which shallow material rollouts
#   still do not pilot or value.
# - Gruul over-rating remains; this could be referee bias, field composition, the
#   censored validation target, or a combination.
# CAVEATS: N=4 matched buckets; the recovered field is synergy-heavy (Golgari x12,
#   Spirits x8 of 32) and still missing aggro/midrange decks blocked by engine-feature
#   cards (Once Upon a Time, Soul-Scar, Torbran, Tarmogoyf). The target is top-finish-
#   censored, and relaxed tier still permits approximated shocklands. Robust claim:
#   across the checked fields VMC does not track the MTGTop8 conversion proxy.
#
# DECISION (per the plan's step 5): VMC failing after the payoff-fidelity fix points
# away from more VMC-targeted deck optimization. Next choices are validation repair /
# field-breadth first, or a referee arm interpreted with a regression-to-mean control.
